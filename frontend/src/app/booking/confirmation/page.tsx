import { BookingConfirmation } from "@/features/appointments/components/booking-confirmation";

export const metadata = {
  title: "Agendamento confirmado | Barbearia",
};

export default function BookingConfirmationPage() {
  return <BookingConfirmation />;
}
